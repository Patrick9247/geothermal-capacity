APP_NAME=地热产能计算 API
# 默认数据库在 backend/data/geothermal.db；如需覆盖，请使用绝对 SQLite URL。
CORS_ORIGINS=http://localhost:5173
JWT_SECRET_KEY=请替换为至少32位随机字符串
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=120
# 多实例部署时每个后端实例须设置不同的节点号（0–1023）
SNOWFLAKE_WORKER_ID=1
